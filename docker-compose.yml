services:
  # Base de datos PostgreSQL especializada para alquileres V2
  postgres_v2:
    image: postgres:15
    container_name: alugueisV2_postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    volumes:
      - postgres_v2_data:/var/lib/postgresql/data
      - ./database/init-scripts:/docker-entrypoint-initdb.d
      - ./database/backups:/backups
    ports:
      - "5432:5432"  # Puerto estándar de PostgreSQL
    networks:
      - alugueisV2_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U alugueisv2_usuario -d alugueisv2_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Adminer para gestión de base de datos
  adminer_v2:
    image: adminer:latest
    container_name: alugueisV2_adminer
    environment:
      ADMINER_DEFAULT_SERVER: postgres_v2
      ADMINER_DESIGN: pepa-linha
    ports:
      - "8080:8080"  # Puerto diferente al sistema V2
    networks:
      - alugueisV2_network
    depends_on:
      postgres_v2:
        condition: service_healthy
    restart: unless-stopped

  # Backend FastAPI
  backend_v2:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: alugueisV2_backend
    env_file:
      - ./.env
    environment:
      DATABASE_URL: ${DATABASE_URL}
      SECRET_KEY: ${SECRET_KEY}
      CSRF_SECRET_KEY: csrf_secret_key_for_alugueis_v2_system_2025
      DEBUG: ${DEBUG}
    # Elimina esta línea:
    # CORS_ALLOW_ORIGINS: ${CORS_ALLOW_ORIGINS}
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    networks:
      - alugueisV2_network
    depends_on:
      postgres_v2:
        condition: service_healthy
    restart: unless-stopped
    labels:
      # Labels para Traefik (solo activos si USE_TRAEFIK=true)
      - "traefik.enable=${USE_TRAEFIK:-false}"
      - "traefik.http.routers.alugueisv2-backend.rule=Host(`${BACKEND_DOMAIN:-api.alugueis.local}`) && PathPrefix(`/api`)"
      - "traefik.http.routers.alugueisv2-backend.entrypoints=websecure"
      - "traefik.http.routers.alugueisv2-backend.tls.certresolver=cloudflare"
      - "traefik.http.services.alugueisv2-backend.loadbalancer.server.port=8000"

  # Frontend Optimizado
  frontend_v2:
    image: nginx:alpine
    container_name: alugueisV2_frontend
    volumes:
      - ./frontend:/usr/share/nginx/html
      - ./nginx-frontend.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "3000:80"
    networks:
      - alugueisV2_network
    depends_on:
      - backend_v2
    restart: unless-stopped
    labels:
      # Labels para Traefik (solo activos si USE_TRAEFIK=true)
      - "traefik.enable=${USE_TRAEFIK:-false}"
      - "traefik.http.routers.alugueisv2-frontend.rule=Host(`${FRONTEND_DOMAIN:-alugueis.local}`)"
      - "traefik.http.routers.alugueisv2-frontend.entrypoints=websecure"
      - "traefik.http.routers.alugueisv2-frontend.tls.certresolver=cloudflare"
      - "traefik.http.services.alugueisv2-frontend.loadbalancer.server.port=80"

volumes:
  postgres_v2_data:
    driver: local

networks:
  alugueisV2_network:
    driver: bridge
